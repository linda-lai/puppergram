<%= form_with(model: pupper, local: true) do |form| %>
  <% if pupper.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pupper.errors.count, "error") %> prohibited this pupper from being saved:</h2>

      <ul>
      <% pupper.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :picture %>
    <%= form.text_field :picture %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
